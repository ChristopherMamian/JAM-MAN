<div class="jumbotron text-center">
  <div class="container-fluid">
    <h1><%= @category %></h1>
    <h3>Buy stuff that doesn't exist.</h3><br>
    <a class="btn btn-primary btn-lg" href='../../articles/new' role="button">Post a new article now</a>
  </div>
</div>

<div class="container-fluid">
  <ol>
  <% @articles.each do |article| %>
    <li><a href="../../articles/<%= article.id %>"><%= article.title %></a></li>
  <% end %>
  </ol>
</div>
